import {Header} from '../../components/header';import {Footer} from '../../components/footer';import {Home} from '../../components/home';import {CategoryPage,ProductPage,CartPage,WishlistPage,Checkout} from '../../components/pages';import {Account,ContentPage} from '../../components/content-page';
export default async function Page({params}:{params:Promise<{slug?:string[]}>}){const {slug=[]}=await params;let view:React.ReactNode=<Home/>;if(slug[0]==='category')view=<CategoryPage name={slug[1]||'all styles'}/>;else if(slug[0]==='product')view=<ProductPage slug={slug[1]||'1'}/>;else if(slug[0]==='cart')view=<CartPage/>;else if(slug[0]==='wishlist')view=<WishlistPage/>;else if(slug[0]==='checkout')view=<Checkout/>;else if(['account','login','signup','profile','orders'].includes(slug[0]))view=<Account/>;else if(slug.length)view=<ContentPage slug={slug[0]}/>;return <><Header/>{view}<Footer/></>}
